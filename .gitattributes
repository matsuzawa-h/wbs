# Windows batch scripts must keep CRLF on checkout — cmd.exe mis-parses
# LF-only .bat/.cmd (labels/blocks/redirection), which silently aborts
# the release scripts. Pin their line endings regardless of core.autocrlf.
*.bat text eol=crlf
*.cmd text eol=crlf
